# ==========================================
# 1. ??????? (???? username ? password)
# ==========================================
spring.datasource.url=jdbc:mysql://localhost:3306/ljc_games?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=020222
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ==========================================
# 2. ??????? (???? schema.sql ? data.sql)
# ==========================================
# ???????? (????????????????????????????)
spring.sql.init.mode=always
# ???????? (?)
spring.sql.init.continue-on-error=false

# ==========================================
# 3. MyBatis ?? (?? Java ? XML)
# ==========================================
# ?? MyBatis ?? Entity ???? (?????? XML ???)
mybatis.type-aliases-package=ljc.entity
# ?? MyBatis ?? XML ????? (?????? mapper ???)
mybatis.mapper-locations=classpath:mapper/*.xml
# ???????? (??? user_id -> Java userId)
mybatis.configuration.map-underscore-to-camel-case=true

# ==========================================
# 4. ???? (????)
# ==========================================
# ?? SQL ???????
logging.level.ljc.dao=debug