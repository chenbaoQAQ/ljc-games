<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ljc.mapper.UserGeneralMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_general (user_id, template_id, name, status, max_hp, current_hp)
        VALUES (#{userId}, #{templateId}, #{name}, #{status}, #{maxHp}, #{currentHp})
    </insert>

</mapper>