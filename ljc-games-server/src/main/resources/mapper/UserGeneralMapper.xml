<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ljc.mapper.UserGeneralMapper">

    <select id="selectById" resultType="ljc.entity.UserGeneral">
        SELECT * FROM user_general WHERE id = #{id}
    </select>

    <select id="selectByUserId" resultType="ljc.entity.UserGeneral">
        SELECT * FROM user_general WHERE user_id = #{userId}
    </select>

    <select id="selectAvailable" resultType="ljc.entity.UserGeneral">
        SELECT * FROM user_general
        WHERE user_id = #{userId}
          AND status = 'HEALTHY'
          AND is_active = 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_general (user_id, template_id, level, current_hp, max_hp, atk, capacity, status, is_active, personality)
        VALUES (#{userId}, #{templateId}, #{level}, #{currentHp}, #{maxHp}, #{atk}, #{capacity}, #{status}, #{isActive}, #{personality})
    </insert>

    <update id="updateStatus">
        UPDATE user_general
        SET current_hp = #{hp}, status = #{status}
        WHERE id = #{id}
    </update>

</mapper>