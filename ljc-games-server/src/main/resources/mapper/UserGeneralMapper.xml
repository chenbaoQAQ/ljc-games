<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ljc.mapper.UserGeneralMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_generals
        (user_id, template_id, unlocked, activated, level, tier, current_hp, max_hp, rest_turns, capacity,
         equip_weapon_id, equip_armor1_id, equip_armor2_id, equip_shoes_id, equip_flag_id, equip_talisman_id, created_at)
        VALUES
            (#{userId}, #{templateId}, #{unlocked}, #{activated}, #{level}, #{tier}, #{currentHp}, #{maxHp}, #{restTurns}, #{capacity},
             #{equipWeaponId}, #{equipArmor1Id}, #{equipArmor2Id}, #{equipShoesId}, #{equipFlagId}, #{equipTalismanId}, NOW())
    </insert>

    <update id="update">
        UPDATE user_generals
        SET unlocked = #{unlocked},
            activated = #{activated},
            level = #{level},
            tier = #{tier},
            current_hp = #{currentHp},
            max_hp = #{maxHp},
            rest_turns = #{restTurns},
            capacity = #{capacity},
            equip_weapon_id = #{equipWeaponId},
            equip_armor1_id = #{equipArmor1Id},
            equip_armor2_id = #{equipArmor2Id},
            equip_shoes_id = #{equipShoesId},
            equip_flag_id = #{equipFlagId},
            equip_talisman_id = #{equipTalismanId}
        WHERE id = #{id}
    </update>

    <select id="selectByUserId" resultType="ljc.entity.UserGeneralTbl">
        SELECT * FROM user_generals WHERE user_id = #{userId}
    </select>
    <select id="selectById" resultType="ljc.entity.UserGeneralTbl">
        SELECT * FROM user_generals WHERE id = #{id}
    </select>
</mapper>