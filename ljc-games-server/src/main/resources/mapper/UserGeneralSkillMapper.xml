<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ljc.mapper.UserGeneralSkillMapper">
    <insert id="insert">
        INSERT INTO user_general_skill (general_id, current_skill_id, updated_at)
        VALUES (#{generalId}, #{currentSkillId}, NOW())
    </insert>
</mapper>