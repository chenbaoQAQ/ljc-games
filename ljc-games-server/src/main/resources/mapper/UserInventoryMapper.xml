<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ljc.mapper.UserInventoryMapper">

    <update id="decreaseItem">
        UPDATE user_inventory
        SET count = count - #{count}
        WHERE user_id = #{userId} AND item_id = #{itemId} AND count >= #{count}
    </update>

    <select id="selectCount" resultType="java.lang.Integer">
        SELECT count FROM user_inventory WHERE user_id = #{userId} AND item_id = #{itemId}
    </select>
    
    <select id="selectByUserId" resultType="ljc.entity.UserInventoryTbl">
        SELECT * FROM user_inventory WHERE user_id = #{userId}
    </select>

</mapper>
