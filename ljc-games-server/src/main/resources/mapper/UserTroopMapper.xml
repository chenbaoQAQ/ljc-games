<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ljc.mapper.UserTroopMapper">

    <select id="selectAll" resultType="ljc.entity.UserTroop">
        SELECT * FROM user_troops WHERE user_id = #{userId}
    </select>

    <select id="selectByType" resultType="ljc.entity.UserTroop">
        SELECT * FROM user_troops WHERE user_id = #{userId} AND type = #{type}
    </select>

    <insert id="addStock">
        INSERT INTO user_troops (user_id, type, count)
        VALUES (#{userId}, #{type}, #{count})
        ON DUPLICATE KEY UPDATE count = count + #{count}
    </insert>

    <update id="decreaseStock">
        UPDATE user_troops
        SET count = count - #{delta}
        WHERE user_id = #{userId}
          AND type = #{type}
          AND count >= #{delta}
    </update>

</mapper>