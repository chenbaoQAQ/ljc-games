<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ljc.mapper.UserProfileMapper">

    <select id="selectById" resultType="ljc.entity.UserProfile">
        SELECT * FROM user_profile WHERE user_id = #{userId}
    </select>

    <update id="update">
        UPDATE user_profile
        SET gold = #{gold}, diamond = #{diamond}, unlocked_countries = #{unlockedCountries}
        WHERE user_id = #{userId}
    </update>

    <update id="decreaseGold">
        UPDATE user_profile
        SET gold = gold - #{delta}
        WHERE user_id = #{userId}
          AND gold >= #{delta}  </update>

</mapper>